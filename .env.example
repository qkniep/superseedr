# This is an example file.
# To use, copy this file to '.env' and fill in your values.
# For vpn configurations checkout .gluetun.env.example

# -----------------------------------------------------------------
# --- User Configuration ---
# -----------------------------------------------------------------

# --- 1. Image Selection ---
# Uncomment ONE of these to choose your pre-built image.
IMAGE_NAME=jagatranvo/superseedr:latest    # Public build (default)
#IMAGE_NAME=jagatranvo/superseedr:private   # Private build

# --- 2. Download Directory (Recommended) ---
# Map the container's download folder to a folder on your host.
# This is where your downloaded files will appear.

# Linux/macOS Example:
# HOST_SUPERSEEDR_DATA_PATH=~/Downloads/superseedr

# Windows Example:
# HOST_SUPERSEEDR_DATA_PATH=C:\Users\YourName\Downloads\superseedr


# --- 3. Configuration & State (Advanced) ---
# It is recommended to leave these COMMENTED OUT.
# Docker will automatically create isolated volumes for your settings.
#
# ⚠️ WARNING: Do NOT point these to your existing native config folders
# (e.g. ~/.config/jagalite.superseedr). Sharing config files between
# Native and Docker versions will cause path errors.
#
# Only uncomment these if you need to manually edit settings.toml or
# access logs from the host. Use a fresh, empty directory.

# HOST_SUPERSEEDR_CONFIG_PATH=./docker-data/config
# HOST_SUPERSEEDR_SHARE_PATH=./docker-data/share

# --- Port Parameters ---
# Static port for providers that do not support programmatic port forwarding.
# Gluetun and superseedr will both use this port.
# CLIENT_PORT=6881

# -----------------------------------------------------------------
# --- Developer Configuration ---
# -----------------------------------------------------------------
# For developers building locally with 'docker compose --build':
# Set this to 'true' to compile the private version of the app.
#PRIVATE_BUILD=false
